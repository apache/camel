- route:
    id: cron-log
    from:
      uri: timer:cron
      parameters:
        period: "5000"
      steps:
        - setBody:
            simple: "Scheduled task running at ${date:now:HH:mm:ss}"
        - log: "${body}"
