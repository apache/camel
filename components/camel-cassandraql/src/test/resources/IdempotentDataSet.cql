CREATE TABLE CAMEL_IDEMPOTENT (
  ID varchar,
  KEY varchar,
  PRIMARY KEY (ID, KEY)
);
INSERT INTO CAMEL_IDEMPOTENT(ID, KEY)
    VALUES('ID','Add_Exists');
INSERT INTO CAMEL_IDEMPOTENT(ID, KEY)
    VALUES('ID','Contains_Exists');
INSERT INTO CAMEL_IDEMPOTENT(ID, KEY)
    VALUES('ID','Remove_Exists');
