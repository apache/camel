## ---------------------------------------------------------------------------
## Licensed to the Apache Software Foundation (ASF) under one or more
## contributor license agreements.  See the NOTICE file distributed with
## this work for additional information regarding copyright ownership.
## The ASF licenses this file to You under the Apache License, Version 2.0
## (the "License"); you may not use this file except in compliance with
## the License.  You may obtain a copy of the License at
##
##      http://www.apache.org/licenses/LICENSE-2.0
##
## Unless required by applicable law or agreed to in writing, software
## distributed under the License is distributed on an "AS IS" BASIS,
## WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
## See the License for the specific language governing permissions and
## limitations under the License.
## ---------------------------------------------------------------------------
#set( $default = "#[[##default]]#" )
/*
 * Licensed to the Apache Software Foundation (ASF) under one or more
 * contributor license agreements.  See the NOTICE file distributed with
 * this work for additional information regarding copyright ownership.
 * The ASF licenses this file to You under the Apache License, Version 2.0
 * (the "License"); you may not use this file except in compliance with
 * the License.  You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Generated by camel build tools - do NOT edit this file!
 */
package ${package};

import java.util.Base64;
import java.util.List;
import java.util.function.BiConsumer;
import javax.annotation.processing.Generated;

#set( $pkgs = $mojo.newTreeSet() )
#set( $mojoClassName = $mojo.getClass().getName() )
#foreach( $clazz in $model )
    #set( $foo = $pkgs.add($clazz.getPackageName()) )
#end
#foreach( $pkg in $pkgs )
#if( $pkg != "java.lang" && $pkg != "org.apache.camel" )
import ${pkg}.*;
#end
#end

@Generated("${mojoClassName}")
@SuppressWarnings({"deprecation","rawtypes"})
public class JavaDslModelWriter extends JavaDslModelWriterSupport {

## ---- Public write methods for root elements ----
#foreach( $clazz in $model )
    #if( $mojo.getXmlRootElement( $clazz ) )
        #set( $name = ${clazz.getSimpleName()} )
        #set( $element = $mojo.getXmlRootElement($clazz).name() )
    public String write${name}(${name} def) {
        resetState();
        StringBuilder sb = new StringBuilder();
        #if( $name == "RouteDefinition" )
        writeRoute(sb, def);
        #else
        beginStep(sb, "${element}", def);
        doWrite${name}(sb, def);
        #end
        return sb.toString();
    }
    #end
#end

    public String writeOptionalIdentifiedDefinitionRef(OptionalIdentifiedDefinition def) {
        resetState();
        StringBuilder sb = new StringBuilder();
        doWriteOptionalIdentifiedDefinitionRef(sb, def);
        return sb.toString();
    }

## ---- Per-class doWrite methods ----
#set( $elementRefs = $mojo.newClassTreeSet() )
#foreach( $clazz in $model )
    #if( $clazz.simpleName == "OptionalIdentifiedDefinition" )
        #set( $foo = $elementRefs.add($clazz) )
    #end
    #if( !$mojo.getXmlEnum($clazz) && !$clazz.isInterface() )
        #set( $hasDerived = false )
        #foreach( $c in $model )
            #if( $c.getSuperclass() == $clazz )
                #set( $hasDerived = true )
                #break
            #end
        #end
        #set( $name = $clazz.simpleName )
        #set( $gname = $mojo.getGenericSimpleName($clazz) )
        #set( $members = $mojo.getProperties($clazz).toList() )
        #set( $thisAttributes = $mojo.getAttributes($clazz).toList() )
        #set( $superWithAttributes = false )
        #foreach( $cl in $mojo.getClassAndSuper($clazz.getSuperclass()).toList() )
            #if( $mojo.getAttributes($cl).findAny().isPresent() )
                #set( $superWithAttributes = $cl )
                #break
            #end
        #end
        #set( $thisElements = $mojo.getElements($clazz).toList() )
        #set( $superWithElements = false )
        #foreach( $cl in $mojo.getClassAndSuper($clazz.getSuperclass()).toList() )
            #if( $mojo.getElements($cl).findAny().isPresent() )
                #set( $superWithElements = $cl )
                #break
            #end
        #end
        #if( !$thisAttributes.isEmpty() || !$thisElements.isEmpty() || $mojo.getXmlRootElement($clazz) || $mojo.isReferenced($clazz, $model) )
## ---- Macro: write attributes ----
            #macro( writeJavaDslAttrs )
                #if( $superWithAttributes )
        doWrite${superWithAttributes.getSimpleName()}Attributes(sb, def);
                #end
                #foreach( $member in $thisAttributes )
                    #if( $member.type.getName() == "java.lang.String" )
        doWriteAttribute(sb, "${member.attributeName}", def.${member.getter}(), ${member.defaultValue});
                    #else
        doWriteAttribute(sb, "${member.attributeName}", toString(def.${member.getter}()), ${member.defaultValue});
                    #end
                #end
            #end
## ---- Macro: write elements ----
            #macro( writeJavaDslElems )
                #if( $superWithElements )
        doWrite${superWithElements.getSimpleName()}Elements(sb, def);
                #end
                #foreach( $member in $thisElements )
                    #set( $list = $member.genericType.rawClass.name == "java.util.List" )
                    #if( $list )
                        #set( $root = $member.genericType.getActualTypeArgument(0).rawClass )
                    #else
                        #set( $root = $member.genericType.rawClass )
                    #end
                    #if( $member.xmlElementRefs )
        // TODO: @XmlElementRefs: ${member}
                    #elseif( $member.xmlElementRef )
                        #set( $foo = $elementRefs.add($root) )
                        #if( $list )
                            #if( $member.name == "outputs" )
        doWriteOutputs(sb, def.${member.getter}(), this::doWrite${root.simpleName}Ref);
                            #else
                                #set( $refName = $member.xmlElementRef.name() )
                                #if( $refName && $refName != $default && $refName != "" )
        doWriteChildList(sb, "${refName}", def.${member.getter}(), this::doWrite${root.simpleName}Ref);
                                #else
        doWriteChildList(sb, "${member.name}", def.${member.getter}(), this::doWrite${root.simpleName}Ref);
                                #end
                            #end
                        #else
                            #if( $member.name == "expression" && $root.simpleName == "ExpressionDefinition" )
        doWriteExpressionRef(sb, def.${member.getter}());
                            #else
        doWriteElementRef(sb, "${member.name}", def.${member.getter}(), this::doWrite${root.simpleName}Ref);
                            #end
                        #end
                    #elseif( $member.xmlElements )
                        #if( $list )
        if (def.${member.getter}() != null) {
            for (var item : def.${member.getter}()) {
                switch (item.getClass().getSimpleName()) {
                            #foreach( $elem in $member.xmlElements.value() )
                                #set( $t = $elem.type().simpleName )
                    case "${t}" -> doWriteChildElement(sb, "${elem.name()}", (${t}) item, this::doWrite${t});
                            #end
                }
            }
        }
                        #else
        if (def.${member.getter}() != null) {
            switch (def.${member.getter}().getClass().getSimpleName()) {
                            #foreach( $elem in $member.xmlElements.value() )
                                #set( $t = $elem.type().simpleName )
                case "${t}" -> doWriteChildElement(sb, "${elem.name()}", (${t}) def.${member.getter}(), this::doWrite${t});
                            #end
            }
        }
                        #end
                    #elseif( $member.xmlElement )
                        #set( $t = $root.simpleName )
                        #set( $n = $member.xmlElement.name() )
                        #if( $n == $default )
                            #set( $n = ${member.name} )
                        #end
                        #if( $list )
                            #set( $w = false )
                            #if( $member.xmlElementWrapper )
                                #set( $w = $member.xmlElementWrapper.name() )
                            #end
                            #if( $t == "String" )
        doWriteStringList(sb, #if($w)"${w}"#{else}null#end, "${n}", def.${member.getter}());
                            #else
        doWriteChildList(sb, "${n}", def.${member.getter}(), this::doWrite${t});
                            #end
                        #else
                            #set( $adapter = $member.xmlJavaTypeAdapter )
                            #if( $adapter )
                                #set( $cl = $adapter.value() )
                                #set( $actualType = false )
                                #foreach( $m in $cl.declaredMethods )
                                    #if( $m.name == "marshal" && $m.returnType.simpleName != "Object" )
                                        #set( $actualType = $m.returnType )
                                        #break
                                    #end
                                #end
                                #if( !$actualType )
                  ERROR: Unable to determine property name for JAXB adapted member: ${member}
                                #end
        doWriteChildElement(sb, "${n}", new ${cl.simpleName}().marshal(def.${member.getter}()), this::doWrite${actualType.simpleName});
                            #else
                                #if( $t == "String" )
        if (def.${member.getter}() != null) {
            doWriteAttribute(sb, "${n}", def.${member.getter}(), null);
        }
                                #else
        doWriteChildElement(sb, "${n}", def.${member.getter}(), this::doWrite${t});
                                #end
                            #end
                        #end
                    #elseif( $member.xmlAnyElement )
        // @XmlAnyElement - not applicable for Java DSL
                    #else
                        #set( $t = $root.simpleName )
                        #set( $n = $member.xmlRootElement.name() )
                        #if( $n == $default )
                            #set( $n = ${member.name} )
                        #end
                        #if( $list )
                            #if( $t == "String" )
        doWriteStringList(sb, null, "${n}", def.${member.getter}());
                            #else
        doWriteChildList(sb, "${n}", def.${member.getter}(), this::doWrite${t});
                            #end
                        #else
                            #if( $t == "String" )
        if (def.${member.getter}() != null) {
            doWriteAttribute(sb, "${n}", def.${member.getter}(), null);
        }
                            #else
        doWriteChildElement(sb, "${n}", def.${member.getter}(), this::doWrite${t});
                            #end
                        #end
                    #end
                #end
            #end
## ---- Generate Attributes method (if class has derived classes) ----
            #if( $hasDerived && !$thisAttributes.isEmpty() )
    protected void doWrite${name}Attributes(StringBuilder sb, ${gname} def) {
                #writeJavaDslAttrs
    }
            #end
## ---- Generate Elements method (if class has derived classes) ----
            #if( $hasDerived && !$thisElements.isEmpty() )
    protected void doWrite${name}Elements(StringBuilder sb, ${gname} def) {
                #writeJavaDslElems
    }
            #end
## ---- Generate main doWrite method ----
    protected void doWrite${name}(StringBuilder sb, ${gname} def) {
            #if( $hasDerived && !$thisAttributes.isEmpty() )
        doWrite${name}Attributes(sb, def);
            #else
                #writeJavaDslAttrs
            #end
            #set( $value = $mojo.getValues($mojo.getClassAndSuper($clazz)).findFirst() )
            #if( $value.isPresent() )
        doWriteValue(sb, def.${value.get().getter}());
            #end
            #if( $hasDerived && !$thisElements.isEmpty() )
        doWrite${name}Elements(sb, def);
            #else
                #writeJavaDslElems
            #end
    }
        #end
    #end
#end

## ---- Polymorphic dispatch methods ----
#foreach( $clazz in $elementRefs )
    #set( $qname = $clazz.simpleName )
    protected void doWrite${qname}Ref(StringBuilder sb, ${qname} v) {
        if (v != null) {
            switch (v.getClass().getSimpleName()) {
    #foreach( $cl in $model )
        #if( $mojo.getXmlRootElement($cl) && $mojo.getClassAndSuper($cl).toList().contains($clazz) )
            #set( $t = $cl.simpleName )
            #set( $n = $mojo.getXmlRootElement($cl).name() )
            #if( $n == $default )
                #set( $n = $mojo.lowercase($t) )
            #end
                case "${t}" -> {
                    beginStep(sb, "${n}", v);
                    doWrite${t}(sb, (${t}) v);
                    endStep(sb, "${n}", v);
                }
        #end
    #end
            }
        }
    }
#end
}
