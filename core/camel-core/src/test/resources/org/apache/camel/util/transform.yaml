- route:
    id: myRoute
    from:
      uri: direct:start
      steps:
        - transform:
            id: transform1
            simple:
              expression: "Hello ${body}"
        - to:
            id: myMock
            uri: mock:result
